# 야간 백로그 (마중 / JB 본선 — 우선순위 순)

> 매일 새벽 클라우드 루틴(Claude Code Routine)이 위에서부터 처리. 완료 시 `- [ ]` → `- [x]`.
> 각 항목은 표시/문서/테스트/연출/발표자료 레이어 중심. 송금 실행 경로(게이트/룰/AML) 로직은 절대 손대지 않는다.
> 7/3 코드 동결 이후에는 본 백로그의 코드 변경 항목을 처리하지 않는다(문서/오탈자만).
> 루틴 작동법: `OVERNIGHT_ROUTINE.md` 참조.

## 동결 구역 보호 규칙 (위반 시 그 변경 즉시 폐기)
- 동결 구역 수정 금지: Gate A/B/C 순서·단일 실행 경로(orchestrator.run_remittance), rules.py 판정, mcp_tools의 execute_remittance/screen_beneficiary_aml, lock/unlock·busyRef, 오프라인 폴백(healthRef 분기), paced 타이밍, suppressNotifs, api.js throw 금지 계약, 교차 고정 수치(FX 18.12/18.45, 대환 13.59%·연 246만, AML 컷 40/70).
- main 직접 커밋·푸시 금지. 작업은 **본선 브랜치 `claude/finals-prototype-prep-02e3b6`** 에서만. 사람이 아침에 PR diff를 리뷰한다.
- 기존 파일 편집이 잘리는 환경이면 셸 직접 쓰기(cat > 파일) 후 `wc -l` 검증.
- 매 항목 후 검증 통과 필수. 실패하면 `git checkout -- .` 되돌리고 PROGRESS_LOG에 '실패·보류' 기록.

## P0 실무진 체감 — 위법·보안 리스크 제거 (문서, 최우선·무위험)
> 실무진이 데모를 "책상에 올리기" 위한 전제조건. 출처·근거는 docs/실무진_공감_분석.md.
- [x] (위험 없음) 전금법 "접근매체 양도 아님" 방어 슬라이드/문서 1장: 철회권+유효기간+범위→확정적 이전 아님(대법원 2011도16167), 자동이체 약정과 동급. 문서만. → 완료 6/27(docs/본선_발표덱.md S8)
- [x] (위험 없음) "LLM에 흐르는 데이터=자연어 발화뿐, 계좌·실명·금액은 결정적 코드 내부" 데이터흐름도 + 금융위 2024.8 망분리 로드맵 정렬 멘트. 문서만. → 완료 6/27(발표덱 S9)
- [x] (위험 없음) "mock→JB 실데이터/인증 연동" 매핑 슬라이드(한패스 레일·esign 본인확인·DSR·신용등급·거시KPI가 운영 시 무엇으로 대체되는지). 문서만. → 완료 6/27(발표덱 S10)

## P1 체감도 — 컴플라이언스 화면 (저위험, 표시·읽기 레이어)
- [x] (위험 낮음) AML "보류 사유 설명 패널": score 분해를 한국어로(예: 75 = 위임범위밖 30 + 신규수취인 25 + 고액 20). 기존 flags·score 표출만, 룰 무변경. 검증: vite build. → 완료 6/27(format.flagKoWeighted, AdminDashboard STR 큐)
- [x] (위험 낮음) STR 큐 행에 검토/보고/기각 액션 버튼(모의) + 담당자·처리시각 컬럼 — '워크플로의 일부'로 보이게. 표시만. 검증: vite build. → 완료 6/27(AdminDashboard 조치 컬럼+strActions)
- [x] (위험 낮음) CDD·CTR 배지를 게이트 트레이스에 추가(고객확인 완료 / 현금 아님→CTR 비대상, 모의 표기). 특금법 3종 완비 신호. 표시만. 검증: vite build. → 완료 6/27(Cards.GatePills)
- [ ] (위험 낮음) STR 큐 카드에 점수·플래그·차단사유를 'JB 컴플라이언스 심사창구' 톤으로 표시 다듬기(컬럼: 거래·점수·플래그·상태·사유). 표시만, 데이터는 기존 api.strQueue. 검증: `npx vite build --outDir /tmp/nightly-build --emptyOutDir`.
- [x] (위험 낮음) 감사로그(audit) 항목의 event_type을 한국어 라벨로 매핑 표시(gate_a→위임검증, gate_c→AML 등). 표시만. 검증: vite build. → 완료 6/27(format.eventKo)
- [ ] (위험 낮음) 대시보드 상단에 '검증·개선: 설명가능성·책임소재' 헤더 카피와 적재 건수 가독성 개선. 표시만. 검증: vite build.
- [ ] (위험 낮음) BusinessValuePanel 검증·개선 카드의 '임계값 제안'이 자동적용 아님(위임장 갱신 동의 전제)을 명시 문구로 강조. 표시만. 검증: vite build.
- [ ] (위험 낮음) 사업가치 콘솔에 전환율 3/5/7/10% 민감도 표 토글(보수/기준/공격) — '7% 한 점 베팅' 인상 제거. demoData 정적값 기반 표시. 검증: vite build.

## P1 체감도 — 실데이터 루프 (위험 중, 읽기전용 백엔드 — 사람 머지 권장)
- [!→리뷰] (위험 중) GET /metrics/loop 읽기전용 집계 엔드포인트: 최근 N건 audit_log의 execute 이벤트 수·오탐0·평균 환율우위→임계값 재계산 '제안값' 반환. 송금 실행 경로 호출 금지(순수 SELECT). 검증: `pytest -q`(테스트 추가 22 목표). ※읽기전용·게이트 불변 확인 후 사람이 머지.
- [ ] (위험 낮음) 위 엔드포인트 완료 후 검증·개선 카드를 실데이터로 렌더(없으면 기존 정적값 폴백 유지). 표시 레이어. 검증: vite build.

## P2 데모 보강 (저위험, 표시 레이어)
- [x] (위험 낮음) E-9 페르소나 첫 장면: 고객 폰 첫 진입에 '입국 시 브로커 사채 1,500만·연 30%' 한 줄 + 베트남어 인사 강화(CustomerChat 헤더/GREETING, 표시만). 검증: vite build. → 완료 6/27(App.jsx GREETING)
- [ ] (위험 낮음) LLM 의도파싱 안내 1컷: '금액은 후보일 뿐, 게이트가 위임장으로 재검증' 카드 1장(표시 push만, 송금 플로우 불변). 검증: vite build.
- [ ] (위험 낮음) 대환 4블록 고지에서 절약액·월상환액·연체가산이 동일 크기로 보이는지 표시 점검·정렬 보강(표시만). 검증: vite build.
- [ ] (위험 낮음) 접근성 패스: aria-label, 포커스 순서, 색 대비(WCAG AA) 점검(표시 레이어). 검증: vite build.

## P2 문서 — 출처·규제 정합
- [ ] (위험 없음) 출처맵·README의 추정값/확정값 분리 표기 일관성 패스(대환 693억·연이자 66.5억·전환7%=자체추정 / 연 246만=확정 명시). 문서만. 검증: 불필요.
- [ ] (위험 없음) 계열사 시너지 확장 매트릭스 문서 1개(광주은행·JB우리캐피탈로 동일 위임엔진 확장). 문서만. 검증: 불필요.
- [ ] (위험 없음) diagrams.md에 3중게이트 단일 다이어그램 + 메타포 캡션('LLM은 통역, 금고 열쇠는 결정적 코드') 정리. 문서만. 검증: 불필요.

## P2 발표 자료 (문서/산출물)
- [x] (위험 없음) 본선 라이브 발표덱 뼈대 markdown 초안(장면형 오프닝 15초 페르소나→3중게이트→1→2단계 깔때기 퍼널→규제 내장 착지). 문서만. → 완료 6/27(docs/본선_발표덱.md)
- [x] (위험 없음) 1단계 송금=2단계 대환 깔때기 단일 퍼널 슬라이드 문구·수치(출처맵 기준) 정리. 문서만. → 완료 6/27(발표덱 S7)
- [ ] (위험 없음) Q&A 방어카드 6~8종 30초 답변 보강(docs/본선_QnA_방어카드.md 갱신). 문서만.
- [ ] (위험 없음) 라이브 시연 런북 + 재시드 체크리스트 + 해상도·단축키 점검표 갱신. 문서만.
- [ ] (위험 없음) build_pptx.py / build_docx.py 빌드 통과 확인 및 산출물 재생성. 검증: 스크립트 실행 성공.

## P3 테스트 (게이트 로직 불변, 테스트만)
- [ ] (위험 낮음) 백엔드 엣지 케이스 1~2건 추가: 위임 범위 밖+AML PASS→자동실행 금지(held) 경로, FX 임계 경계값. 게이트 로직 불변. 검증: `pytest -q`(개수 유지·증가).
- [ ] (위험 낮음) parse_intent 출력 스키마 계약 테스트(LLM 미연동 환경에서도 그린). 검증: pytest.

## 보류 (사람 리뷰 필수, 야간 자동 금지)
- [!] llm.py parse_intent의 Claude(claude-opus-4-8) 실연동 — 외부 API키 연동·동결 구역 인접. 사람이 직접 작성·키 관리·머지.
- [!→리뷰] (위험 중) DSR 실값 계산: mcp_tools.py:267 하드코딩 0.32 → 급여(소득)·loans_external(부채)로 산출해 prescreen_refi(dsr=)에 주입. 룰(rules.py) 불변·호출부만. 여신담당 최대 약점. 검증: pytest. ※대환 경로 인접 → 사람 리뷰 후 머지.
- [!→리뷰] (위험 중) 등급별 대환 금리 테이블(가A/B/C) + 절약액 '연=단리 1년치' 각주/다년 보정. 데이터·표시 레이어, 룰 불변. 검증: pytest+vite build. ※금소법 정확고지 직결 → 사람 리뷰.
- [!] 고객 화면 검증·개선 카드의 송금 finishOutcome 수정 — 동결 구역 인접.
- [!] 게이트/룰/AML 로직 변경 일체.
- [!] main 머지, 외부 API 키 연동, 라이브러리 추가, 대규모 리팩터.
- [!] GET /metrics/loop의 main 머지 — 읽기전용·게이트 불호출 검증 후 사람 승인.
